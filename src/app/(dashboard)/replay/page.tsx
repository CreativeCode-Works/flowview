export const metadata = {
  title: "Journey Replay — FlowView",
};

export default function ReplayPage() {
  return (
    <div>
      <h1 className="mb-2 text-2xl font-bold text-white">Journey Replay</h1>
      <p className="text-sm text-zinc-400">
        Pick any contact to see their full journey across your entire automation
        stack — every tag, every automation, every charge — on one timeline.
      </p>
    </div>
  );
}
